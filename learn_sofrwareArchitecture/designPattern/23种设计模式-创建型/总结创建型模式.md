# 总结创建型模式

## 简单工厂模式

传入类型返回对象，用户无需关心对象的创建过程，违背开闭

## 简单工厂模式

抽象工厂定义创建一种产品的接口，添加一个新产品只需添加一个对应工厂即可。符合开闭，但是会导致类突增加

## 抽象工厂模式

抽象工厂模式定义创建多种产品的接口，当只能创建一个产品时就退化为了工厂方法模式。增加产品族符合开闭，增加产品等级结构不符合开闭。适合产品等级结构稳定的场景
